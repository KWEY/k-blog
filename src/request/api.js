/**
 * 接口域名的管理
 */
const base_url = 'http://api.webq.top/static';
const base = {
  typeList: `${base_url}/db/type.db.json`,
  directoryList: `${base_url}/db/directory.json`,
  article: `${base_url}/db/`,
  mseInfo: `${base_url}/db/doc/mse.xml`,
  mseMenu: `${base_url}/db/doc/mse-list.json`,
};

export default base;
